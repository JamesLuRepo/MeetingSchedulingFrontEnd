<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.comp2100_6442_androidproject">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".activities.OneKeySchedule"
            android:label="Schedule results"/>
        <activity android:name=".activities.ModifyMeeting"
            android:label="meeting modification"/>
        <activity android:name=".activities.UserList"
            android:label="Choose a user"/>
        <activity
            android:name=".activities.GroupInfo"
            android:label="Group information" />
        <activity android:name=".activities.MeetingInfo"
            android:label="Meeting information"/>
        <activity
            android:name=".activities.MeetingAdd"
            android:label="Add new meetings" />
        <activity
            android:name=".activities.GroupAdd"
            android:label="Add new groups" />
        <activity
            android:name=".activities.TimeSlotAdd"
            android:label="Add your time slot" />
        <activity
            android:name=".activities.Settings"
            android:label="Settings" />
        <activity android:name=".activities.LogInPage"
            android:label="Please login"/>
        <activity android:name=".activities.SignUpPage"
            android:label="Please sign up"/>
        <activity
            android:name=".activities.TimeSlotInfo"
            android:label="Your time slots"
            android:parentActivityName=".activities.Settings" />
        <activity android:name=".activities.HomePage"
            android:label="Groups and meetings">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
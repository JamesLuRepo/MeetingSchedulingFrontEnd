<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.comp2100_6442_androidproject">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.GroupAdd"
            android:label="Add new groups"/>
        <activity
            android:name=".activities.PreferenceAdd"
            android:label="Add your preferences"/>
        <activity
            android:name=".activities.Settings"
            android:label="Settings" />
        <activity android:name=".activities.LogInPage" />
        <activity android:name=".activities.SignUpPage" />
        <activity
            android:name=".activities.PreferenceDetail"
            android:label="Preference"
            android:parentActivityName=".activities.Settings" />
        <activity android:name=".activities.HomePage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>